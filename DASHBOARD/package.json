{
  "name": "railsync-dashboard",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Delhi Division Section Controller dashboard — Vite + Tailwind + FastAPI",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "backend": "cd backend && uvicorn main:app --reload --port 8000",
    "data": "python build_data.py"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.7.0",
    "vite": "^6.3.0"
  },
  "dependencies": {
    "lucide-static": "^1.41.0",
    "zustand": "^5.0.0"
  }
}
